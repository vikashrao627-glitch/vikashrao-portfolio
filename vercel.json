{
  "buildCommand": "cd artifacts/portfolio && BASE_PATH=/ pnpm install && BASE_PATH=/ pnpm run build",
  "outputDirectory": "artifacts/portfolio/dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "BASE_PATH": "/"
  }
}
